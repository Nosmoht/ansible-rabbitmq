---
- include_vars: '{{ item }}'
  with_first_found:
  - 'default.yml'

- include: install.yml
  tags: rabbitmq_install

- include: service.yml
  tags: rabbitmq_service

- include: user.yml
  tags: rabbitmq_user
